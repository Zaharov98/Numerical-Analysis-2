
import math
import numpy as np


def get_schema_table(x, t, h, et):
    """
    :param x: np.array of x values
    :param t: np.array of t values
    :param h: step for X
    :param et: step for T
    :return: filled solution schema table
    """
    table = np.zeros(shape=(len(x), len(t)))
    return table
